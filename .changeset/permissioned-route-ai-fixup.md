---
'@backstage/migrate-permissioned-route': minor
---

Add optional AI fixup step for edge cases the AST codemod cannot handle mechanically (re-exports, type annotations, dynamic element props). Enable with `--param aiFixup=true`.
