schema_version: '1.0'

name: '@backstage/mui4-to-bui-migration-recipe'
version: '0.2.2'
description: 'MUI 4 to BUI: Migration recipe that runs every deterministic MUI→BUI codemod from the registry in a safe order.'
author: 'Paul Schultz<pschultz@redhat.com>'
license: 'Apache-2.0'
workflow: 'workflow.yaml'

targets:
  languages: ['tsx', 'ts', 'jsx', 'js', 'css', 'json']

keywords: ['backstage', 'migration', 'mui', 'bui', 'recipe', 'workflow']

registry:
  access: 'public'
  visibility: 'public'

capabilities: []
