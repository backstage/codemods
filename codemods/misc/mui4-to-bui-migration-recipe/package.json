{
  "name": "@backstage/mui4-to-bui-migration-recipe",
  "version": "0.2.2",
  "description": "MUI 4 to BUI: Migration recipe that runs every deterministic MUI→BUI codemod from the registry in a safe order.",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "codemod": "1.12.13"
  }
}
