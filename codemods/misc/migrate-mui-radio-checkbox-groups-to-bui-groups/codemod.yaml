schema_version: '1.0'

name: '@backstage/migrate-mui-radio-checkbox-to-bui'
version: '0.1.0'
description: 'MUI 4 to BUI: Replace radio and checkbox group patterns with BUI groups'
author: 'Paul Schultz<pschultz@redhat.com>'
license: 'Apache-2.0'
repository: 'https://github.com/backstage/codemods'
workflow: 'workflow.yaml'

targets:
  languages: ['tsx', 'ts']

keywords: ['backstage', 'migration', 'mui', 'bui', 'radio', 'checkbox', 'groups', 'bui']

registry:
  access: 'public'
  visibility: 'public'

capabilities: []
