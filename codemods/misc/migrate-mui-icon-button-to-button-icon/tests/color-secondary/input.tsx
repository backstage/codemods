import IconButton from '@material-ui/core/IconButton';

const MyComponent = () => (
  <IconButton aria-label="settings" color="secondary" onClick={handleSettings}>
    <SettingsIcon />
  </IconButton>
);
