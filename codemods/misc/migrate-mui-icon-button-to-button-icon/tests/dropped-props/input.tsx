import IconButton from '@material-ui/core/IconButton';

const MyComponent = () => (
  <IconButton aria-label="menu" size="small" edge="start" color="inherit" onClick={handleMenu}>
    <MenuIcon />
  </IconButton>
);
