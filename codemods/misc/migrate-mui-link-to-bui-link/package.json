{
  "name": "@backstage/migrate-mui-link-to-bui-link",
  "version": "0.1.0",
  "description": "MUI 4 to BUI: Replace Material-UI Link with BUI Link",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.13"
  }
}
