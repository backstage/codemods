{
  "name": "@backstage/migrate-mui-layout-to-bui-layout",
  "version": "0.2.1",
  "description": "MUI 4 to BUI: Convert common MUI layout primitives to BUI layout",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.13"
  }
}
