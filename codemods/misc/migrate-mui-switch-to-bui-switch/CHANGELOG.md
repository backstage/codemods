# @backstage/migrate-mui-switch-to-bui-switch

## 0.1.0

### Minor Changes

- Initial release: migrate MUI `Switch` (and `FormControlLabel` wrapping `Switch`) to Backstage UI `Switch` during the MUI 4 to BUI migration.
