import Switch from '@material-ui/core/Switch';

const MyComponent = () => (
  <>
  {/* TODO(backstage-codemod): finish switch migration manually (color) */}
  <Switch color="primary" checked={on} />
</>
);
