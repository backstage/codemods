import { Text } from '@backstage/ui';

const MyComponent = () => (
  <>
  {/* TODO(backstage-codemod): verify Text variant manually (gutterBottom) */}
  <Text variant="title-x-small">Section Title</Text>
</>
);
