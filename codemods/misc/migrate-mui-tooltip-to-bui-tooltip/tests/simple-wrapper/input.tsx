import Tooltip from '@material-ui/core/Tooltip';

const MyComponent = () => (
  <Tooltip title="More actions">
    <IconButton aria-label="more">
      <MoreVertIcon />
    </IconButton>
  </Tooltip>
);
