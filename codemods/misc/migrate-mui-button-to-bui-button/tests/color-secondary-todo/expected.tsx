import Button from '@material-ui/core/Button';

const MyComponent = () => (
  <>
  {/* TODO(backstage-codemod): verify Button intent manually (color-secondary) */}
  <Button variant="contained" color="secondary">Delete</Button>
</>
);
