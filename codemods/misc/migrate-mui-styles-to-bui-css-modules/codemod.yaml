schema_version: '1.0'

name: '@backstage/migrate-mui-styles-to-bui-css-modules'
version: '0.1.0'
description: 'MUI 4 to BUI: Migrate makeStyles usage to BUI CSS modules'
author: 'Paul Schultz<pschultz@redhat.com>'
license: 'Apache-2.0'
repository: 'https://github.com/backstage/codemods'
workflow: 'workflow.yaml'

targets:
  languages: ['tsx', 'ts', 'css']

keywords: ['backstage', 'migration', 'mui', 'bui', 'styles', 'css', 'modules']

registry:
  access: 'public'
  visibility: 'public'

capabilities:
  - fs
