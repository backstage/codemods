{
  "name": "@backstage/migrate-mui-styles-to-bui-css-modules",
  "version": "0.1.0",
  "description": "MUI 4 to BUI: Migrate makeStyles usage to BUI CSS modules",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml && bash ./scripts/assert-css-goldens.sh"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.3"
  }
}
