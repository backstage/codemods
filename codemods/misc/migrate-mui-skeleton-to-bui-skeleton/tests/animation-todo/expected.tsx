import Skeleton from '@material-ui/lab/Skeleton';

const MyComponent = () => (
  <>
  {/* TODO(backstage-codemod): finish skeleton migration manually (animation) */}
  <Skeleton animation="wave" width={80} height={16} />
</>
);
