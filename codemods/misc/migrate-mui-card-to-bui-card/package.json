{
  "name": "@backstage/migrate-mui-card-to-bui-card",
  "version": "0.1.0",
  "description": "MUI 4 to BUI: Replace Card family with BUI Card",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.13"
  }
}
