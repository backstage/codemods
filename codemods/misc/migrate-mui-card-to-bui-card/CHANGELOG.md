# @backstage/migrate-mui-card-to-bui-card

## 0.1.0

### Minor Changes

- Initial release: migrate MUI `Card` / `CardContent` / `CardActions` / simple `CardHeader` to Backstage UI Card family during the MUI 4 to BUI migration.
