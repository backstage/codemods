{
  "name": "@backstage/migrate-mui-select-family-to-bui-select",
  "version": "0.2.0",
  "description": "MUI 4 to BUI: Replace Select wrapper patterns with BUI Select",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.3"
  }
}
