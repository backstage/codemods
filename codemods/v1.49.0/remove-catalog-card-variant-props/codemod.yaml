schema_version: '1.0'

name: '@backstage/remove-catalog-card-variant-props'
version: '0.1.0'
description: 'Backstage 1.49.0: Remove deprecated variant and gridSizes props from catalog card components'
author: 'Paul Schultz<pschultz@redhat.com>'
license: 'MIT'
repository: 'https://github.com/backstage/codemods'
workflow: 'workflow.yaml'

targets:
  languages: ['tsx', 'ts']

keywords: ['backstage', 'migration', 'catalog', 'variant', 'gridSizes', 'plugin-catalog', '1.49.0']

registry:
  access: 'public'
  visibility: 'public'

capabilities: []
