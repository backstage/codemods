import { css } from '@emotion/css';

const bgStyles = css`
  background: var(--bui-bg);
  &:hover {
    background: var(--bui-bg-tint-hover);
  }
`;

const inlineStyle = `background: var(--bui-bg-tint)`;
