import { Avatar } from '@backstage/ui';

export const Example = () => (
  {/* TODO(backstage-codemod): Avatar render prop removed, review custom rendering */}
  <Avatar src="/photo.jpg" name="Jane" size="x-large" />
);
