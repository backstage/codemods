{
  "name": "@backstage/rename-header-to-plugin-header",
  "version": "0.2.1",
  "description": "Backstage 1.48.0: Rename Header to PluginHeader, HeaderProps to PluginHeaderProps, HeaderDefinition to PluginHeaderDefinition in @backstage/ui",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.3"
  }
}
