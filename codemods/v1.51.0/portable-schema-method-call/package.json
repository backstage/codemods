{
  "name": "@backstage/portable-schema-method-call",
  "version": "0.2.0",
  "description": "Backstage 1.51.0: Call PortableSchema.schema() as a method instead of property access",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.3"
  }
}
