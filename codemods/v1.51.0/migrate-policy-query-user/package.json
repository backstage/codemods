{
  "name": "@backstage/migrate-policy-query-user",
  "version": "0.2.1",
  "description": "Backstage 1.51.0: Migrate PolicyQueryUser off removed token, expiresInSeconds, and identity fields",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l tsx ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "dependencies": {
    "@jssg/utils": "^0.0.8"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.3"
  }
}
