schema_version: '1.0'

name: '@backstage/migrate-permissioned-route'
version: '0.3.1'
description: 'Migrate PermissionedRoute to Route + RequirePermission for @backstage/plugin-permission-react'
author: 'Alex Bit<alex@codemod.com>'
license: 'Apache-2.0'
repository: 'https://github.com/backstage/codemods'
workflow: 'workflow.yaml'

targets:
  languages: ['typescript', 'tsx']

keywords: ['backstage', 'migration', 'permission-react', 'PermissionedRoute', 'RequirePermission', '1.50.0']

registry:
  access: 'public'
  visibility: 'public'

capabilities: []
