schema_version: '1.0'

name: '@backstage/remove-bootstrap-env-proxy'
version: '0.1.0'
description: 'Backstage 1.50.0: Remove deprecated bootstrapEnvProxyAgents() call and import from @backstage/cli-common'
author: 'Paul Schultz<pschultz@redhat.com>'
license: 'MIT'
workflow: 'workflow.yaml'

targets:
  languages: ['tsx', 'ts']

keywords: ['backstage', 'migration', 'cli-common', 'proxy', '1.50.0']

registry:
  access: 'public'
  visibility: 'public'

capabilities: []
