import type { CatalogClient, CatalogProcessingExtensionPoint } from '@backstage/plugin-catalog-node';


function setup(client: CatalogClient, processing: CatalogProcessingExtensionPoint) {
  // use types
}
