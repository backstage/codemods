import type { CatalogClient } from '@backstage/plugin-catalog-node';
import type { CatalogProcessingExtensionPoint } from '@backstage/plugin-catalog-node/alpha';

function setup(client: CatalogClient, processing: CatalogProcessingExtensionPoint) {
  // use types
}
