{
  "name": "@backstage/migrate-valkey-config",
  "version": "0.2.1",
  "description": "Backstage 1.46.0: Migrate Valkey cache configuration to new native options",
  "type": "module",
  "scripts": {
    "test": "yarn exec codemod jssg test -l yaml ./scripts/codemod.ts ./tests && yarn exec codemod workflow validate -w workflow.yaml"
  },
  "devDependencies": {
    "@codemod.com/jssg-types": "1.6.2",
    "codemod": "1.12.3"
  }
}
