backend:
  cache:
    store: 'valkey' # cache backend
    connection: redis://localhost:6379
    client:
      namespace: 'my-app' # app namespace
      keyPrefixSeparator: ':' # separator
      clearBatchSize: 1000 # batch size
      useUnlink: false # unlink flag
