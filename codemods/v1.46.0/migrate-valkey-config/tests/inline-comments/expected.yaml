backend:
  cache:
    store: 'valkey' # cache backend
    connection: redis://localhost:6379
    client:
      keyPrefix: 'my-app:'
    # Migrated by @backstage/migrate-valkey-config — Valkey native options in 1.46
