schema_version: '1.0'

name: '@backstage/v1-46-0-migration-recipe'
version: '0.2.0'
description: 'Backstage 1.46.0: Migration recipe that runs every @backstage v1.46.0 codemod from the registry in a safe order.'
author: 'Paul Schultz<pschultz@redhat.com>'
license: 'MIT'
workflow: 'workflow.yaml'

targets:
  languages: ['json', 'yaml']

keywords: ['backstage', 'migration', 'recipe', 'workflow', 'upgrade', '1.46.0']

registry:
  access: 'public'
  visibility: 'public'

capabilities: []
